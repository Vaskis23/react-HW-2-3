class Money {
    constructor (amount, currency) {
        this.amount = amount
        this.currency = currency
    }

    //HW encapsulate both: amount, currency
    //don't allow negativw amount
    //allow only 'EUR', 'MDL', 'USD'  
}

export {Money}