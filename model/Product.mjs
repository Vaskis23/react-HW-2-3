class Product {

    constructor (id, name, image, price) {
        this.id = id,
        this.name = name
        this.image = image
        this.price = price
    }
    //HW encapsulate 'name' property
    //don't allow empty product name
    //if name is empty - trow an error with message "invalid product namr"

}

export {Product}